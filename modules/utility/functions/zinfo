# zinfo shows information about various services
#
# Running tmux sessions
# perl path (for perlbrew)
# ruby path (for rvm)
# node path (for nvm)
# is boot2docker running
# running containers
#
# By andrew williams
# https://github.com/skarfacegc


zinfo () {
    echo
    echo -e "$FG[45]Perl: \t$FG[49]" `which perl`
    echo -e "$FG[45]Ruby: \t$FG[49]" `which ruby`
    echo -e "$FG[45]Node: \t$FG[49]" `which node`
}
